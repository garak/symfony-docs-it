extra_fields_message
~~~~~~~~~~~~~~~~~~~~

**tipo**: ``stringa`` **predefinito**: ``This form should not contain extra fields.``

Il messaggio di errore usato se i dati inviati al form
contengono campi che non fanno parte della definizione del form stesso. Si
può usare il segnaposto ``{{ extra_fields }}`` per mostrare un elenco
di nomi di campi extra.
